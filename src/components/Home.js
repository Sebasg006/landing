import React from "react";
import Navbar from "./Navbar";
const Home = (props) => {
    return(<div className="Home-container">
        <h1>{props.title}</h1>
        <h1>Testeo</h1>
        </div>
    )
}
export default Home;